# fail2ban filter：匹配 homepage 写入 auth.log 的登录失败行。
# 行格式： 2026-06-05 17:00:00 LOGIN FAILED ip=1.2.3.4 user=xxx
[Definition]
# fail2ban 会先识别并剥离行首时间戳，故 failregex 不写日期前缀，直接匹配正文。
failregex = LOGIN FAILED ip=<HOST> user=\S+
ignoreregex =
